object S37_Set extends  App {
  // won't support duplicates
  val s1 = Set("Apple", "Orange", "Mango", "Orange", "Banana")
  println(s1)

  // unlike list, set doesn't support indexes
  // s1(0) won't work, we cannot access element by index
}
